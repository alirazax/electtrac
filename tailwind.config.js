/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js}",  // This will include all HTML and JS files inside the 'src' folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
